# Local secrets and VPN configs
/.vpn/
/vpn/*.ovpn

# Allow the example file but not real credentials
!/vpn/example-client.ovpn

# Ignore raw auth files that may be created locally
/vpn/auth.txt
/.vpn/auth.txt

# Ignore decrypted SSH tarballs and temporary artifacts (moved to secrets/)
secrets/ssh_keys.tar
secrets/ssh_keys
secrets/ssh_keys.*.tmp
